import { NightSkyConfiguration } from '~/models/visuals';

export const NightskyConfig: NightSkyConfiguration = {
  color: '#ffffff',
  minRadius: 0.75,
  maxRadius: 2,
  minOpacity: 50,
  maxOpacity: 100,
  minSpeed: 0.005,
  maxSpeed: 0.125,
  starCount: 150,
};
