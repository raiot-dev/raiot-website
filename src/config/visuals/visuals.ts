import { NightSkyConfiguration } from '~/models/visuals';

export const NightskyConfig: NightSkyConfiguration = {
  color: '#ffffff',
  minRadius: 1,
  maxRadius: 2.5,
  minOpacity: 50,
  maxOpacity: 100,
  minSpeed: 0.005,
  maxSpeed: 0.125,
  starCount: 75,
};
